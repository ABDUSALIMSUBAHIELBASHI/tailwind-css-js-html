const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');
const dotenv = require('dotenv');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');

dotenv.config();

const app = express();

// Middleware
app.use(cors());
app.use(express.json());

// MongoDB Connection - Using your new database name
mongoose.connect(process.env.MONGODB_URI || 'mongodb://localhost:27017/abdu-salim-portfolio')
.then(() => console.log('✅ MongoDB Connected to abdu-salim-portfolio database'))
.catch(err => console.log('❌ MongoDB Connection Error:', err));

// ... rest of your code remains the same ...
# Access MongoDB container
sudo docker exec -it mongodb mongosh

# Then run the same commands:
show dbs
use abdu-salim-portfolio
show collections
# Check if MongoDB is running
sudo systemctl status mongod
# or
sudo docker ps

# Check if port 27017 is accessible
nc -zv localhost 27017

# Test MongoDB connection directly
mongosh --eval "db.runCommand({connectionStatus: 1})"mongosh

use abdu-salim-portfolio

# Check users collection
db.users.find().pretty()

# Check messages collection (after contact form submissions)
db.messages.find().pretty()cd ~/2tailwind.page/abdu-salim-portfolio
npx http-server -p 8000
PORT=5000
MONGODB_URI=mongodb://localhost:27017/abdu_portfolio
JWT_SECRET=your-super-secret-jwt-key-change-this
